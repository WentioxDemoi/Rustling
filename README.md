# Rust-Task-Manager
The goal is to learn Rust by doing a simple project

# Notions Actuelles
- Utilisation de Cargo pour tout (équivalent NPM mais pour Rust) (cargo new ...) (cargo run)
- Installation de rust-analyzer via brew pour l'auto completion + l'extension VSCode